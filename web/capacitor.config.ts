import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'mini-app-react',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
